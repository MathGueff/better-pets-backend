$kind: http-request
name: List Animals
method: GET
url: "{{baseUrl}}/animals"
order: 1000
queryParams:
  - key: page
    value: "1"
    disabled: true
    description: Page number (starts at 0)
  - key: limit
    value: "2"
    disabled: true
    description: Items per page (max 50)
